{
  "version": 2,
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/next" },
    { "src": "backend/vercel-handler.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/backend/vercel-handler.ts" },
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}
